// Vérification du micro, du haut-parleur et de la caméra avant d'entrer dans la classe.
export function TestMicroCamera(_props: { camera?: boolean; onPret?: (ok: boolean) => void; nuit?: boolean }) {
  return null; // Implémenté par le module visio.
}
