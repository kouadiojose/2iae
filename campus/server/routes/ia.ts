// Assistant pédagogique IA : questions, fiches de révision, génération de quiz, aide à la correction, résumés.
import type { Express } from "express";

export function enregistrerIa(_app: Express) {
  // À implémenter.
}
