// Côté étudiant : écoute la radio du cours (≈ 15 Mo/h), reprise automatique après coupure.
export function LecteurRadio(_props: { seanceId: number; nuit?: boolean; onConsommation?: (octets: number) => void }) {
  return null; // Implémenté par le module visio.
}
