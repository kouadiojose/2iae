// Compte : profil, préférences, première connexion, mot de passe oublié, abonnement push.
import type { Express } from "express";

export function enregistrerCompte(_app: Express) {
  // À implémenter.
}
