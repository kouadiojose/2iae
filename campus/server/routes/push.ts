// Notifications push (PWA) : clé publique VAPID, abonnement et désabonnement de l'appareil.
import type { Express } from "express";

export function enregistrerPush(_app: Express) {
  // À implémenter.
}
