// Côté formateur : diffuse le son du micro en « radio » (flux HTTP très léger) aux étudiants en ligne.
export function EmetteurRadio(_props: { seanceId: number; flux: MediaStream | null; actif: boolean }) {
  return null; // Implémenté par le module visio.
}
